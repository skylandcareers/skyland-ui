import { metadata } from './metadata';
import { ReactNode } from 'react';

export { metadata };

export default function AustraliaPRLayout({
  children,
}: {
  children: ReactNode;
}) {
  return <>{children}</>;
} 