import { metadata } from './metadata';
import { ReactNode } from 'react';

export { metadata };

export default function GermanyPRLayout({
  children,
}: {
  children: ReactNode;
}) {
  return <>{children}</>;
} 