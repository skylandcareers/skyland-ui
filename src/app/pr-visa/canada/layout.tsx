import { metadata } from './metadata';
import { ReactNode } from 'react';

export { metadata };

export default function CanadaPRLayout({
  children,
}: {
  children: ReactNode;
}) {
  return <>{children}</>;
} 