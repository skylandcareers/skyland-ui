import { metadata } from './metadata';
import { ReactNode } from 'react';

export { metadata };

export default function SlovakiaLayout({
  children,
}: {
  children: ReactNode;
}) {
  return <>{children}</>;
} 