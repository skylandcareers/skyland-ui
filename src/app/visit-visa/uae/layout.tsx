import { metadata } from './metadata';
import { ReactNode } from 'react';

export { metadata };

export default function uaeVisitVisaLayout({
  children,
}: {
  children: ReactNode;
}) {
  return <>{children}</>;
} 