import { metadata } from './metadata';
import { ReactNode } from 'react';

export { metadata };

export default function SchengenVisitVisaLayout({
  children,
}: {
  children: ReactNode;
}) {
  return <>{children}</>;
} 