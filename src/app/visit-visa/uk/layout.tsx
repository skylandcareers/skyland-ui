import { metadata } from './metadata';
import { ReactNode } from 'react';

export { metadata };

export default function UKVisitVisaLayout({
  children,
}: {
  children: ReactNode;
}) {
  return <>{children}</>;
} 