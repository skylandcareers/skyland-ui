import { metadata } from './metadata';
import { ReactNode } from 'react';

export { metadata };

export default function UKPRLayout({
  children,
}: {
  children: ReactNode;
}) {
  return <>{children}</>;
} 