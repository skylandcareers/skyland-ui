import { metadata } from './metadata';
import { ReactNode } from 'react';

export { metadata };

export default function NewZealandPRLayout({
    children,
}: {
  children: ReactNode;
}) {
  return <>{children}</>;
} 